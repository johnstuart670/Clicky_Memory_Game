import { Card, CardImage} from 'mdbreact';

<Card className = "col-md-3 mx-auto">
		<CardImage className="img-fluid mx-0 px-0 my-0 py-0" 
		src={props.img} 
		id = {props.id}
		onClick = {}/>
</Card>

export default Card